package io.github.yuroyami.kiteplayer.compose

/**
 * Keeps the deprecated aggregate as a physical KLIB while its public API is supplied entirely by
 * dependencies. This is internal so the migration coordinate adds no new surface of its own.
 */
internal object CompatibilityMarker
