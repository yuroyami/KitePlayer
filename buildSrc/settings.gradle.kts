// Named explicitly so Gradle stops warning that type-safe project accessors would generate
// different code depending on the folder this repository is checked out into.
rootProject.name = "buildSrc"
