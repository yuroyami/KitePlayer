package io.github.yuroyami.kiteplayer.rt

// A source file makes Kotlin Gradle publish an empty main klib alongside the module's cinterop klib.
// It intentionally declares no Kotlin API: kiteplayer-rt's callable surface stays in kitert.
