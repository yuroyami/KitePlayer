# Lines from the pre-split KPKMP.md that were INTENTIONALLY changed, each with its reason.
# The verifier treats these as edited rather than lost.
#
# FORMAT: a line beginning "= " is an allowed edit; the rest of that line is the ORIGINAL text,
# verbatim. Everything else here is a comment. The prefix exists because some of the lines being
# allowed are themselves markdown headings starting with "#", which a plain comment rule would
# swallow, and a silently swallowed entry is how a real loss hides.
#
# Keep this list short and keep the reasons. Every entry is a place where "not one line rewritten"
# was deliberately broken, and each one has to be obviously worth it.
#
# RULE ONE step 5 had to name the log's new home, since section 14 moved to KPKMP-PAST.md.
= > 5. **Log the work.** Section 14 gets what was measured and what was NOT, in the same commit.
#
# The document's own opening claimed one FILE. It is two files and still one document; left alone,
# the first thing a reader met would contradict the file they were holding.
= against the source and merged in. This is the only planning document. Everything the
= executor needs is in this file and in the code.
#
# Skeleton rule 1, same reason: it named KPKMP.md, which no longer exists as a path.
= 1. **KPKMP.md is the only planning document.** If it is not in KPKMP, it is not the plan. Its
#
# The title named the whole document. Each half now titles itself, so a reader who opened one of
# them learns which half it is from line 1 instead of inferring it.
= # KPKMP: the pilot document
